# MKS65C-FinalProject
Final Project for Systems

## Makefile
Go to the ```sys``` folder. Run ```make```. Open a terminal to be the server. Navigate to the ```sys``` directory and type in ```./server```. Open two terminals to be the clients. Naviagate to the ```sys``` directory and run ```./client```. Beat yourself in Connect 4. Run ```make clean``` to get rid of all evidence of your loss.
